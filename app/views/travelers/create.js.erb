<% if @group.errors.any? %>
  # Handle errors
<% else %>
  secondform = '<%= escape_javascript(render '/shared/formtraveler', layout: false ) %>'
  secondformDiv = document.querySelector(".form-traveler");
  secondformDiv.insertAdjacentHTML("beforeEnd", secondform);
  button = document.querySelector(".button-temp");
  button.classList.add("hidden");
  button.classList.remove("button-temp")
<% end %>